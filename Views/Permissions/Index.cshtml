@model IEnumerable<ZaculeuValley.IxchelAdmin.Models.Permission>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PermissionName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PermissionDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PermissionName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PermissionDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idpermission">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idpermission">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idpermission">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
