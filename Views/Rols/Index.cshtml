@model IEnumerable<ZaculeuValley.IxchelAdmin.Models.Rol>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RolName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RolDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RolName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RolDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idrol">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idrol">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idrol">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
